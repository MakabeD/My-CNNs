# TODO: benchmarking system/module
# that includes: time, memory usage, acuraccy , presicion, recall
# experiments in mlflow + dagshub (dagshub.init(repo_owner=repo_owner, repo_name=repo_name, mlflow=True) )with
# repo_owner: str = "MakabeD",
# repo_name: str = "My-CNNs",
# a function that runs a benchmarking loop, and an argument data path (a benchmark set)
